/vendor/
/node_modules/
composer.lock
package-lock.json
.phpunit.cache/
.phpunit.result.cache
.DS_Store

# Python virtualenv used by scripts/
.venv/
__pycache__/
*.py[cod]

# Local ETL artefacts (downloads/intermediate) -- output goes to resources/
scripts/.cache/
scripts/.downloads/

# CI checks ichava/maintainer-toolkit out here (sync-upstream.yml).
# Never track it: a nested repo is recorded as a phantom gitlink.
/.ichava-maintainer-toolkit/
/.phpstan.cache
